<p id="notice"><%= notice %></p>

<% @sounds.each do |sound| %>
  <div class = "col-lg-3 col-md-4 col-sm-6 col-xs-12 sound-card-container">
    <div class = "sound-card">
      <div class = "sound-picture" style = "background-image: url('<%= sound.imageAttachment.url %>');" onClick="handleAudioImageClick('<%= sound.id %>')">
        <!-- <h3 class = "sound-name"><%= sound.name %></h3>
        <p class = "sound-control"><%= link_to 'x', sound, method: :delete, data: { confirm: 'Are you sure you want to delete this sound?' }, :class => "destroy" %></p> -->
      </div>
      <div class = "sound-info">
        <h3><%= sound.name %></h3>
      <!-- <p class = "sound-control"><%= link_to 'Show', sound %></p>
      <p class = "sound-control"><%= link_to 'Edit', edit_sound_path(sound) %></p> -->
        <audio loop id = "<%= sound.id %>-audio">
          <source src = "<%= sound.soundAttachment.url %>" type = "audio/mp3">
        </audio>
        <div>
          <i class="fa fa-play" id = "sound-play-pause-<%= sound.id %>" onClick="handleAudioImageClick('<%= sound.id %>')" aria-hidden="true"></i>
          <div class = "sound-volume-controls">
            <i class="fa fa-volume-up" id = "volume-icon-<%= sound.id %>" aria-hidden="true"></i>
            <input type="range" id="sound-volume-<%= sound.id %>" class = "sound-volume" min="0" value="100" max="100" step="1" oninput="handleVolume('<%= sound.id %>')" onchange="handleVolume('<%= sound.id %>')">
          </div>
        </div>
      </div>
      <!-- <p><%= link_to "Download sound file", sound.soundAttachment.url %></p>
      <p><%= link_to "Download image file", sound.imageAttachment.url %></p> -->
    </div>
  </div>
<% end %>
